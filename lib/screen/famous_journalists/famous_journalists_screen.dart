import 'package:flutter/material.dart';

class FamousJournalistsScreen extends StatelessWidget {
  const FamousJournalistsScreen({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('FamousJournalistsScreen'),
      ),
      body: Column(
        children: [
          Container(),
        ],
      ),
    );
  }
}